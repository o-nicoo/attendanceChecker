# Path to the SQLite database file
DB_PATH=./data/attendance.db

# Optional shared secret — the Raspberry Pi must send this as "secret" in the POST body
# Leave empty to disable authentication (only for local/dev use)
SCAN_SECRET=
